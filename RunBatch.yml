steps:
  - bash: |
      echo "This is a test"
      TestVariable=Azure
      echo "TestVariable:  $TestVariable"
      echo "##vso[task.setvariable variable=TestVariable;isOutput=true]$sd_rootdev"
    # name: RunBatchStep
    displayName: 'Run a multi-line script'
