variables:
  sd_rootdev_output: $[ dependencies.RunBatch.outputs['RunBatch.RunBatchStep.sd_rootdev'] ]

steps:
  - bash: |
      echo "Captured sd_rootdev: $(sd_rootdev_output)"
    displayName: 'Capture sd_rootdev'
