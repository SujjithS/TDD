steps:
  - task: Bash@3
    continueOnError: true
    timeoutInMinutes: 35790
    inputs:
      targetType : inline
      script: |
        echo "$(Push_Param)" | tr '[:upper:]' '[:lower:]'
        Branch_Path = $(echo "$(Push_Param)" | tr '[:upper:]' '[:lower:]')
        echo "Push archive variable is artifactory.boschdevcloud.com/zugspitze-series-docker-local/petalinux-v2021.2:$(Branch_Path)"



